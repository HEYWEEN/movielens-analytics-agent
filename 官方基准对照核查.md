# 官方基准对照：已知内容差异

核查使用 GroupLens MovieLens 1M 官方压缩包（MD5 `c4d9eecfca2ab87c1945afe126590906`）。官方数据仅用于本地对照，**不参与清洗和评分**，也不随本仓库发布。

旧版 `ml1m-rules-v2` 曾将 2003-02-28 零点错当成全天的时间上限，误隔离当天晚些时候的 12 条评分。v3 修正为当日 23:59:59 UTC；当前 v4 清洗后的 1,000,209 条评分与官方文件逐条一致，缺失和额外均为 0。

用户与电影的清洗数量也与官方一致，但完整内容分别有 **126**、**48** 条不同。逐项核查表明，这 174 个 ID 在课程原始文件中都恰有两个变体，官方值与另一个值各出现一次。频次打平，字典序后备规则选中了非官方值；原始行的先后顺序不是原因。用户差异包括 87 个性别及年龄段同时改变、39 个邮编变为短值；电影差异包括 25 个乱码样式标题和 23 个年份格式异常标题。现有字段校验无法仅凭这些文件断定真实值。

v4 保留稳定选择，但将所有同频冲突保留项标记为未核验：用户 **217**、电影 **105**、评分 **0**，总计 **322**。上述 174 个内容差异全部在其中；其余 148 个虽然碰巧与官方一致，仍不能由课程原始文件独立核实。`ml1m-quality-v2` 的 Accurate 对未核验行扣分，因此清洗后为 **99.968** 而非 100。v4 的三个清洗文件与 v3 逐字节一致，未按官方答案修改数据。

若后续决定用官方数据消解冲突，必须先确认使用与再分发权限，将它登记为外部参考源、定义可复现的匹配规则，并重新计算版本和评分。当前报告中的 `reference_audit` 是此前对照结果，不是每次 Hadoop 任务重算的指标。
